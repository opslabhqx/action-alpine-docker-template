---
name: 'Docker Alpine Go Action'
description: 'Docker Alpine Go Action'
runs:
  using: 'docker'
  image: 'docker://ghcr.io/opslabhqx/action-alpine-docker-template/go:latest'
  entrypoint: /bin/bash
  args:
    - -c
    - |
        set -o errexit -o pipefail
        curl -fsSL https://raw.githubusercontent.com/opslabhqx/action-alpine-docker-template/main/go/action.sh | bash -s -- main
