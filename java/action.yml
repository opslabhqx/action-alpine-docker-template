---
name: 'Docker Alpine Java Action'
description: 'Docker Alpine Java Action'
runs:
  using: 'docker'
  image: 'docker://ghcr.io/opslabhqx/action-alpine-docker-template/java:latest'
  entrypoint: /bin/bash
  args:
    - -c
    - |
        set -o errexit -o pipefail
        curl -fsSL https://raw.githubusercontent.com/opslabhqx/action-alpine-docker-template/main/java/action.sh | bash -s -- main
